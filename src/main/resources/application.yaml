spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    # for docker db
#    username: demouser
#    password: demopassword
#    url: jdbc:postgresql://localhost:15432/postgres
    # for local db
    username: postgres
    password: postgres
    url: jdbc:postgresql://localhost:5432/postgres
  jpa:
    open-in-view: true
    show-sql: true
    properties:
      hibernate:
        ddl-auto: none
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQL10Dialect
        order_by.default_null_ordering: last
  flyway:
    validate-on-migrate: false
  security:
    oauth2:
      client:
        registration:
          google:
            clientId: '295125751349-7dp9n0ts4gqalikl3srd4b54ul9mv83s.apps.googleusercontent.com'
            clientSecret: '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'
            scope: 'email,profile,openid'

logging:
  level:
    org.springframework.orm.jpa: DEBUG
    org.springframework.transaction: DEBUG

jwt:
  token:
    secret: jwtdevblog
    expired: 3600000 # 1 hour
